# Martin Tile Server — Configuração
# Imagem: ghcr.io/maplibre/martin:latest
# Auto-publish todas as tabelas do schema rede_bt com coluna geom

listen_addresses: "0.0.0.0:3000"

postgres:
  connection_string: "${DATABASE_URL}"
  pool_size: 10
  auto_publish:
    tables:
      from: rede_bt
